# For local development only.
# In production (unified deployment), the frontend is served by the backend,
# so API calls go to the same origin — no env var needed.
VITE_API_BASE_URL=http://localhost:5000/api
